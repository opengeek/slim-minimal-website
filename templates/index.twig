{% extends 'base.twig' %}
{% set title = 'Hello, World!' %}

{% block body %}
<div class="container">
    <h1>Hello, {{ name }}!</h1>

    <p>{{ app_name }} version {{ app_version }}</p>
</div>
{% endblock body %}
